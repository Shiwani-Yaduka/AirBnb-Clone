DATABASE_URL=sqlite:///./airbnb_clone.db
JWT_SECRET=change-me-in-production
CORS_ORIGINS=["http://localhost:3000"]
# development (default) = plain-HTTP localhost cookie; production = cross-site
# SameSite=None+Secure cookie required for a Vercel frontend + Render backend split.
ENVIRONMENT=development
